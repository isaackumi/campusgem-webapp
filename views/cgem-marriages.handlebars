    {{> header}}

    <div class="marriage-container">
        <div class="inspiration-banner">
            <h1 class="main-header">Campus Gem Marriages</h1>
            <p class="sub-header">
                Embracing love's transformative power, celebrating each couple's unique path to unity and companionship.
            </p>
        </div>

        <div class="marriage-grid">
            {{#each images}}
            <div class="marriage-card">
                <img src="{{this}}" alt="Marriage Photo" loading="lazy">
            </div>
            {{/each}}
        </div>
    </div>

    {{> footer}}


        <style>
        :root {
            --primary-color: #c92f2f;
            --secondary-color: #000000;
            --bg-color: #1a1a1b;
        }

        body {
            background-color: var(--bg-color);
            font-family: 'Inter', sans-serif;
        }

        .marriage-container {
            max-width: 1600px;
            margin: 0 auto;
            padding: 2rem;
        }

        .inspiration-banner {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            text-align: center;
            padding: 3rem 1rem;
            margin-bottom: 2rem;
            border-radius: 15px;
            box-shadow: 0 15px 30px rgba(0,0,0,0.1);
        }

        .main-header{
            color: white
        }

        .sub-header{
            color: white
        }

        .marriage-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 2.5rem;
        }

        .marriage-card {
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 15px 25px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            transform-origin: center;
        }

        .marriage-card:hover {
            transform: scale(1.05);
            box-shadow: 0 20px 35px rgba(0,0,0,0.12);
        }

        .marriage-card img {
            width: 100%;
            height: 400px;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        @media (max-width: 768px) {
            .marriage-grid {
                grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
                gap: 1.5rem;
            }

            .marriage-card img {
                height: 300px;
            }
        }
    </style>


    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const graduateCards = document.querySelectorAll('.marriage-card');
            
            graduateCards.forEach(card => {
                const img = card.querySelector('img');
                
                card.addEventListener('mouseenter', () => {
                    img.style.transform = 'scale(1.1)';
                });
                
                card.addEventListener('mouseleave', () => {
                    img.style.transform = 'scale(1)';
                });
            });
        });
    </script>

